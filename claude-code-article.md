# Claude Code 完全解説：AIが変える開発の世界、大阪弁版

## はじめに

おおきに！Claude Code てのはな、Anthropic が作った AI 駆動のコーディングアシスタント CLI（コマンドラインインターフェース）ツールやねん。2025年にリリースされて、開発者がターミナルから直接 Claude と相談しながら、コードの作成・編集・デバッグ・解析やらなんやら色々できるようになったんや。この記事では Claude Code の概要から実際の使い方、活用のコツまでを丁寧に説明していくで。

---

## 1. Claude Code ってなんやねん

Claude Code てのは、ターミナルで動く AI コーディングアシスタントやねん。ただのチャットボットとちゃうねんぞ。**実際にファイルを読み書きして、コマンドを実行して、Git 操作やったり**と、開発作業を自分で活動するエージェント型の AI ツールなんや。

### 主な特徴

- **ファイルシステムへのアクセス**：プロジェクト内のファイルを読み取り・編集・作成できるんや
- **コマンド実行**：シェルコマンドやテストを実行して、結果を踏まえて次のアクション決めるねん
- **Git 統合**：差分確認、コミット、ブランチ操作なんかを自然言語で指示できるで
- **MCP（Model Context Protocol）対応**：外部サービスや API とシームレスに連携できるんや
- **マルチエージェント対応**：複数のサブエージェント並列実行で複雑なタスク高速処理するで

---

## 2. インストールと初期設定

### 必要な環境やで

- Node.js 18 以上や
- macOS / Linux / Windows（WSL 推奨）や
- Anthropic API キーがいるねん

### インストール方法

```bash
npm install -g @anthropic-ai/claude-code
```

### 初期設定

```bash
# API キーを設定（最初に起動するときに対話形式で設定することもできるで）
export ANTHROPIC_API_KEY="your-api-key-here"

# Claude Code を立ち上げる
claude
```

---

## 3. 基本的な使い方

### 対話モード

ターミナルで `claude` と入れたら対話セッションが始まるやで。

```
$ claude
✓ Welcome to Claude Code

> このプロジェクトの README を読んで、概要を教えてや
```

Claude Code はプロジェクトの構造を把握して、ファイルを読み取りながら答えるで。

### ワンショットモード

```bash
# 特定タスク一行で実行
claude -p "バグを修正してテストを実行してや"
```

### よく使う指示の例

| 指示 | 動き |
|------|------|
| `このコードのバグ直してや` | ファイルを読み取り、問題を見つけて・修正するで |
| `新しい機能追加してや` | 要件を確認して、コード生成・挿入するねん |
| `テスト書いてや` | 既存コード解析して、テストケース作成するで |
| `コードをリファクタリングしてや` | 読みやすさ・効率性を上げるように変更加えるで |
| `このエラーの原因調べてや` | ログやスタックトレース解析して、原因見つけるねん |

---

## 4. スラッシュコマンド

Claude Code には最初から用意されてるスラッシュコマンドがあるで。

```
/help      - ヘルプ表示するで
/clear     - 会話履歴をリセットするねん
/compact   - コンテキスト圧縮（長い会話でトークン節約）や
/doctor    - 環境の診断チェックやで
/init      - プロジェクトに CLAUDE.md 生成するで
/memory    - メモリ（CLAUDE.md）の内容確認するねん
/model     - 使用するモデル変更するで
/review    - コードレビュー実行するやで
/commit    - 変更をコミットするで
```

---

## 5. CLAUDE.md：プロジェクト独自の指示

`CLAUDE.md` ってのはな、プロジェクトのルートディレクトリに置いたら Claude Code がセッション開始時に自動で読み込むファイルやねん。チームのコーディング規約やプロジェクト独自のルール書いておいたら、毎回指示する手間が省けるで。

### CLAUDE.md の例

```markdown
# プロジェクト概要
このプロジェクトは Next.js + TypeScript で作った EC サイトや。

## コーディング規約
- TypeScript の strict モード使うってや
- コンポーネントは関数コンポーネントで書くねん
- テストは Vitest 使用するで
- コミットメッセージは Conventional Commits に従うやで

## よく使うコマンド
- `npm run dev` - 開発サーバー立ち上げるで
- `npm test` - テスト実行するねん
- `npm run build` - 本番ビルドや

## 気をつけること
- `.env` ファイルは絶対にコミットするなよ
- API キーはハードコードすんなや
```

---

## 6. 権限管理とセキュリティ

Claude Code はファイル編集やコマンド実行をするから、ちゃんとした権限管理が大事やで。

### パーミッションモード

- **デフォルトモード**：危険な操作（ファイル削除、外部への書き込みなんか）は確認を求めるで
- **自動承認モード**（`--dangerously-skip-permissions`）：全操作自動承認（CI 環境向け）や

### 安全な使い方のコツ

1. **重要なブランチへの直接プッシュ避ける**：feature ブランチで作業さすんやで
2. **`.gitignore` の活用**：機密ファイルを管理対象外にするねん
3. **コードレビューの習慣**：AI が作ったコードも必ず人間がレビューするでよ
4. **最小権限の原則**：必要な権限だけ与えるんや

---

## 7. MCP（Model Context Protocol）連携

MCP ってのはな、Claude Code を外部サービスと接続するための標準プロトコルや。

### 代表的な MCP サーバー

- **GitHub MCP**：Issue・PR の作成、コードレビュー、リポジトリ操作するで
- **Slack MCP**：チャンネルへメッセージ送信、スレッド管理やで
- **PostgreSQL MCP**：データベースへクエリ実行するねん
- **Filesystem MCP**：拡張ファイルシステム操作するで
- **Web Search MCP**：リアルタイム Web 検索や

### MCP の設定例（`.claude/settings.json`）

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "your-github-token"
      }
    }
  }
}
```

---

## 8. エージェントモードとサブエージェント

Claude Code の強い機能の一つがな、複数のサブエージェント並列実行できるマルチエージェント対応やで。

### 活用シーン

```
> フロントエンドのバグ修正とバックエンドのテスト追加を同時にやってや
```

こんな指示受けたら、Claude Code は複数のサブエージェント生成して、独立したタスク並列処理するんや。大きいリファクタリングや、複数モジュールにまたがる変更を高速に実施できるで。

---

## 9. 実践的な活用例

### レガシーコードの解析と改善

```
> src/ ディレクトリ全体を解析して、技術的負債の問題点を見つけだして、
  優先度付きのリファクタリング計画を作成してや
```

### テスト駆動開発（TDD）サポート

```
> UserService クラスに新しいメソッド `getUsersByRole` 追加したいねん。
  まずテスト書いて、次に実装作成して、テストが通ることを確認してや
```

### CI/CD パイプラインとの統合

GitHub Actions やらなんかと組み合わせて、PR に対して自動的にコードレビューやテスト修正させることもできるで。

```yaml
# .github/workflows/claude-review.yml の例
- name: Claude Code Review
  run: claude -p "この PR のコードをレビューして問題があれば修正してや"
  env:
    ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
```

---

## 10. 料金と使用量の管理

Claude Code は Anthropic API の従量課金で動きます（2025年現在）。

### コスト最適化のコツ

- **`/compact` コマンドの活用**：長い会話でコンテキスト圧縮してトークン節約するで
- **適切なモデルの選択**：軽い仕事には高速・安いモデル（Haiku）使用するねん
- **明確な指示**：曖昧な指示は多くのやり取り生んで、コスト増えるで
- **スコープ絞った作業**：でっかいコードベースより、関連ファイルに絞って作業さすんや

---

## 11. 他のツールとの比較

| ツール | 形式 | 強いとこ | 弱いとこ |
|--------|------|------|------|
| **Claude Code** | CLI | エージェント型、ファイル操作、MCP 連携 | ターミナル操作に慣れが必要や |
| GitHub Copilot | IDE 拡張 | インライン補完、IDE 統合 | エージェント機能は限定的な |
| Cursor | IDE | AI ネイティブなエディタ体験 | 特定 IDE に依存する |
| Devin | Web | 完全自律型エージェント | 高コスト、制御むずいで |

Claude Code の最大の強いんはな、**エディタに頼らず、あらゆる開発環境で使える汎用性**と、**MCP を通じた外部サービス連携の豊富さ**やねん。

---

## 12. ベストプラクティス

### 効果的な指示の出し方

1. **目標明確に**：「コード改善してや」より「パフォーマンス改善するために、N+1 クエリ解消してや」
2. **制約を伝える**：「既存のテスト壊さんようにや」「後方互換性保ってや」
3. **段階的に進める**：大きいタスクは小さいステップに分割して依頼するで
4. **確認を挟む**：重要な変更の前に計画確認さすんや

### チームでの活用

- `CLAUDE.md` をバージョン管理してチームで共有するで
- Claude Code が作ったコードのレビュープロセス確立するねん
- 繰り返し使うタスクはスラッシュコマンドや hooks に登録するやで

---

## まとめ

Claude Code ってのは、ただのコード補完ツール超えた**自律型の開発パートナー**やねん。ファイル操作からコマンド実行、外部サービス連携まで、開発に必要な幅広いタスクこなせるで。

ちゃんと活用したら：
- 繰り返し作業の自動化による**生産性の大幅向上**が期待できるで
- レガシーコードの解析・改善による**技術的負債の削減**ができるねん
- テスト作成の自動化による**コード品質の向上**が実現できるで

AI と人間が一緒に動く開発スタイルはもう現実のもんになっとるし、Claude Code はその最前線にいるツールの一つやねん。まずは小さいタスクから試してみて、ぼちぼち活用範囲広げていくことをおすすめするで。

---

## 参考リンク

- [Claude Code 公式ドキュメント](https://docs.anthropic.com/ja/docs/claude-code)やで
- [Anthropic 公式サイト](https://www.anthropic.com)や
- [Model Context Protocol（MCP）仕様](https://modelcontextprotocol.io)ねん
- [Claude Code GitHub Issues](https://github.com/anthropics/claude-code/issues)やで
